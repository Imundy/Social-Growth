import { Dimensions } from 'react-native';

const styles = {
    signInButton: {
        width: Dimensions.get('screen').width - 32,
        height: 30,
        marginVertical: 16,
        borderWidth: 1,
        borderColor: '#CCC'
    }
};

export default styles;
